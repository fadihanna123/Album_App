import { StyleSheet } from 'react-native';

export const galleryTextStyles = StyleSheet.create({
  txtBox: {
    borderWidth: 0.5,
    borderRadius: 10,
    fontSize: 45,
    textAlign: 'center',
    margin: 10,
    padding: 10,
  },
  name: {
    fontSize: 15,
    fontStyle: 'italic',
    textAlign: 'right',
    marginBottom: 20,
  },
});
