import { StyleSheet } from 'react-native';

export const galleryTextStyles = StyleSheet.create({
  txtBox: {
    borderWidth: 1,
    borderRadius: 10,
    borderColor: 'white',
    fontSize: 35,
    color: 'white',
    textAlign: 'center',
    margin: 10,
  },
  name: {
    fontSize: 20,
  },
});
