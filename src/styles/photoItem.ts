import { StyleSheet } from 'react-native';

export const photoItemStyles = StyleSheet.create({
  galleryContainer: {
    margin: 4,
  },
  galleryPhoto: {
    width: 85,
    height: 100,
    borderColor: '#fff',
    borderWidth: 1,
    borderRadius: 10,
  },
  spinnerTextStyle: {
    color: '#fff',
  },
});
