import { StyleSheet } from 'react-native';

export const selectedPhotoInfoStyles = StyleSheet.create({
  textContainer: {
    backgroundColor: '#000',
    color: '#fff',
    padding: 10,
    marginTop: 10,
    width: '100%',
    lineHeight: 18,
  },
  fileInfoHeading: {
    fontWeight: 'bold',
    fontSize: 20,
    textAlign: 'center',
  },
});
