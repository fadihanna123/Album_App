import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: "row",
    flexWrap: "wrap",
    margin: 40,
    padding: 5,
    flexGrow: 1,
    backgroundColor: "#2EBD6B",
    paddingTop: 10,
  },
});
